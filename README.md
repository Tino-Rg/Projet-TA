# Main Documentation

This GitHub repository is an implementation of 6 models for the Kaggle Leaf 
Classification problem. All code is conform PEP8 using the `black` module. 

## Models

Modular model definition povided by `src.models` package. Fit and predict with a
sklearn pipeline and grid search cross-validation. Run with `main.py`.